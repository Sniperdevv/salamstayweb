# Gitleaks config — extends the default ruleset.
[extend]
useDefault = true

[allowlist]
description = "Design fixtures and vendored skill docs contain example tokens, never real credentials"
paths = [
  '''design-system/.*''',
  '''\.claude/skills/.*''',
]
