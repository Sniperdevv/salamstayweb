{
  "name": "@salamstay/web",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'next build — wired in T0-7'",
    "dev": "echo 'next dev — wired in T0-7'",
    "lint": "echo 'lint — wired in CI'",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no tests yet'",
    "clean": "rm -rf .next .turbo"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
